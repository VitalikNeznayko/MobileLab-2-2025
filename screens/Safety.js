import React from "react";
import {
  View,
  Text,
  Image,
  ScrollView,
  TouchableOpacity,
  StatusBar,
} from "react-native";
import MainView from "../components/MainView";

export default function Safety() {
  return (
    <MainView>
    </MainView>
  );
}
